# ── Backend API URL ────────────────────────────────────────
# Local development — points to the Express backend
VITE_API_URL=http://localhost:8080

# Production — replace with your deployed Render backend URL
# VITE_API_URL=https://your-portfolio-backend.onrender.com
